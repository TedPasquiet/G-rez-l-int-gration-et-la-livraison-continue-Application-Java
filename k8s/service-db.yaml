apiVersion: v1
kind: Service
metadata:
  name: workshop-organizer-db-service
spec:
  selector:
    app: workshop-organizer-db
  ports:
    - protocol: TCP
      port: 5439
      targetPort: 5439
  clusterIP: None
